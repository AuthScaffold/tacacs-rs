name: Build Binary
description: Build an artifact for a target with the package environment from compute-package-names

inputs:
  target:
    description: 'Rust target triple (for example, x86_64-unknown-linux-gnu)'
    required: true
  package-name:
    description: 'Cargo package name to build'
    required: true
  artifact-name:
    description: 'Binary or library target name to build'
    required: true
  artifact-kind:
    description: 'Artifact kind to build (bin or cdylib)'
    required: true

runs:
  using: composite
  steps:
    - name: Build binary
      shell: bash
      run: |
        if [[ "${{ inputs.artifact-kind }}" == "bin" ]]; then
          cargo build \
            ${BUILD_PROFILE_FLAG} \
            --target "${{ inputs.target }}" \
            --package "${{ inputs.package-name }}" \
            --bins \
            ${FEATURE_ARGS}
        elif [[ "${{ inputs.artifact-kind }}" == "cdylib" ]]; then
          cargo build \
            ${BUILD_PROFILE_FLAG} \
            --target "${{ inputs.target }}" \
            --package "${{ inputs.package-name }}" \
            --lib \
            ${FEATURE_ARGS}
        else
          echo "Error: unsupported artifact kind: ${{ inputs.artifact-kind }}"
          exit 1
        fi
