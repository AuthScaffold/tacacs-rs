[package]
name = "tacacsrs-datastore"
description = "Datastore abstraction that supplies the agent with a YANG TACACS+ configuration and change notifications."
version = "0.0.0-dev"
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
anyhow = "1"
async-trait = "0.1"
futures-core = "0.3"
tacacsrs-config = { path = "../tacacsrs_config" }
tokio = { version = "1.52.3", features = ["sync", "macros", "rt"] }
tokio-stream = { version = "0.1", features = ["sync"] }

[dev-dependencies]
tacacsrs-secrets = { path = "../tacacsrs_secrets" }
tokio = { version = "1.52.3", features = ["full"] }

[lints]
workspace = true
