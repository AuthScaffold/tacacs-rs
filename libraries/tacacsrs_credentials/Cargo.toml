[package]
name = "tacacsrs-credentials"
description = "External credential resolution for TACACS+ runtime server configs"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
anyhow = "1"
base64 = "0.22"
rustls-pki-types = "1.14.0"
tacacsrs-config = { path = "../tacacsrs_config" }

[dev-dependencies]
serde_json = "1"

[lints]
workspace = true