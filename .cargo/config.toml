
## Use this build flag to enable tokio-console.
## It shows async tasks and resources from a process that calls console_subscriber::init().
#[build]
#rustflags = ["--cfg", "tokio_unstable"]


# Debug information for each target.
[target.x86_64-pc-windows-msvc]
rustflags = ["-C", "split-debuginfo=packed"]  # Windows PDB files.